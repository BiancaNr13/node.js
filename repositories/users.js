let {} = require('../databases/users')

const getUsers = () =>{
 return bd;
}

const getUsersById = (idUser) => {
return 
const user = bd.filter((usuario) => usuario.id === idUser);
}

module.exports ={
    getUsers,
    getUsersById
}